{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-10-17/server.schema.json",
  "name": "io.github.domdomegg/time-mcp-server",
  "title": "Time MCP Server",
  "description": "A Model Context Protocol (MCP) server that provides current UTC time in RFC 3339 format. Simple, lightweight tool for getting timestamps in AI applications.",
  "version": "{{VERSION}}",
  "packages": [
    {
      "registryType": "nuget",
      "identifier": "TimeMcpServer",
      "version": "{{VERSION}}",
      "transport": {
        "type": "stdio"
      }
    }
  ]
}